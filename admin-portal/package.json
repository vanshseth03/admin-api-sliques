{
  "name": "sliques-admin",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "concurrently": "^8.2.2",
    "date-fns": "^2.30.0",
    "lucide-react": "^0.294.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "sliques-boutique": "file:.."
  },
  "scripts": {
    "start": "set PORT=3002 && react-scripts start",
    "start:api": "cd ../server && node index.js",
    "start:all": "concurrently --names \"API,ADMIN\" --prefix-colors \"yellow,cyan\" \"npm run start:api\" \"npm run start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
